<Page x:Class="WPF_Demo_Slot2.PageDemo5"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF_Demo_Slot2"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="800"
      Width="700" Height="700"
      Title="PageDemo5"
      >

    <Grid>
        <DockPanel Background="LightBlue" HorizontalAlignment="Stretch" Width="NaN" Height="NaN" VerticalAlignment="Stretch">
            <Button DockPanel.Dock="Top" Height="50" Background="Yellow" Foreground="Black" FontSize="18">Top</Button>
            <Button DockPanel.Dock="Bottom" Height="50" Background="Turquoise" Foreground="Black" FontSize="18">Bottom</Button>
            <Button DockPanel.Dock="Left" Width="50" Background="Orange" Foreground="Black" FontSize="18">Left</Button>
            <Button DockPanel.Dock="Right" Width="50" Background="BurlyWood" Foreground="Black" FontSize="18">Right</Button>
            <Button Background="Green" Foreground="White" FontSize="18">Center</Button>
        </DockPanel>
    </Grid>
</Page>
